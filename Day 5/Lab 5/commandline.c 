#include <stdio.h>

void main(int argc, char **argv) {
    // argc tells us how many strings are in argv.
    // argv is an array of strings.

    int i;

    // Print each entry in argv on a separate line.
    for (i = 0; i < argc; i++) {
        printf("%s\n", argv[i]);
    }

}